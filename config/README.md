# Configuration

`matlab_paths.m` derives the repository root from its own location and provides all active MATLAB directories. Run `startup` from `matlab/main` before using the active MATLAB pipeline.

`paths.example.json` and `model_config.example.json` contain repository-relative examples only. Copy and adapt them locally if a workflow needs different datasets; do not add personal or mapped-drive paths.
